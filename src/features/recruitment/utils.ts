/**
 * Utility helper to trigger browser file download from Blob object (PDFs, CSVs, reports).
 * Re-exported from shared utilities for backward compatibility.
 */
export { downloadBlob } from "@/shared/utils/downloadBlob";